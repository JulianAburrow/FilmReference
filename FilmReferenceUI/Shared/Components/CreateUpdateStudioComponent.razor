@inherits StudiosBasePageClass

<MudCardContent>
    <MudTextField Label="Name" @bind-Value="StudioDisplayModel.Name" AutoFocus OnlyValidateIfDirty="true" />
    <MudTextField Label="Description" @bind-Value="StudioDisplayModel.Description" />
    <br />
    <ViewImageComponent ImageData="StudioDisplayModel.Logo" ImageTitle=@($"{StudioDisplayModel.Name} Box Cover") />
    <br />
    <MudFileUpload T="IBrowserFile" FilesChanged="LocalUploadImage" Accept="image/*">
        <ActivatorContent>
            <MudButton HtmlTag="label"
                Variant="Variant.Filled"
                Color="Color.Primary"
                StartIcon="@Icons.Material.Filled.FileUpload">
                Upload File
            </MudButton>
        </ActivatorContent>
    </MudFileUpload>
    @if (StudioDisplayModel.Logo is not null)
    {
        <br />
        @ImageName
        <br />
        <MudButton HtmlTag="label"
            OnClick="LocalRemoveImage"
            Variant="Variant.Filled"
            Color="Color.Error">
            Remove Image
        </MudButton>        
    }
</MudCardContent>