<MudCard Class="ma-2 pa-2" Style="width: 100%; max-width: 350px;">
    <MudCardHeader>
        <MudText Typo="Typo.h6">@Person.FirstName @Person.LastName</MudText>
    </MudCardHeader>

    <MudCardContent>
        @{
            var textToShow = (Person.IsCastMember, Person.IsDirector) switch
            {
                (true, true)   => "Directs as well as acts",
                (false, true)  => "Director",
                (true, false)  => "Cast Member",
                _              => string.Empty
            };
        }  
        <MudText>@textToShow</MudText>
        <MudText>@Person.Description</MudText>
        <br />
        <ViewImageComponent ImageData="@Person.Picture" ImageTitle="@($"{Person.FirstName} {Person.LastName} Photo")" />
    </MudCardContent>

    <MudCardActions Class="d-flex flex-wrap" Style="gap: 8px;">
        <MudButton Variant="Variant.Filled" Color="Color.Primary" Href=@($"/person/view/{Person.PersonId}")>
            View
        </MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" Href=@($"/person/edit/{Person.PersonId}")>
            Edit
        </MudButton>
        @if (Person.Films?.Count == 0 && Person.FilmPerson.Count == 0)
        {            
            <MudButton Variant="Variant.Filled" Color="Color.Error" Href=@($"/person/delete/{Person.PersonId}")>
                Delete
            </MudButton>
        }
    </MudCardActions>
</MudCard>