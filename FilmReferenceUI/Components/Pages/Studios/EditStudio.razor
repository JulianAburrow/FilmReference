@page "/studio/edit/{StudioId:int}"
@inherits StudiosBasePageClass

<PageTitle>Edit Studio</PageTitle>

@if (!_isLoaded)
{
    <LoadingComponent DisplayValue="@(_entityNotFound
                                        ? StudioNotFoundMessage
                                        : LoadingStudioMessage)"
                      IsLoading="@(StudioModel.StudioId != 0)" />
    return;
}

<MudContainer Class="mt-1 px-8" MaxWidth="MaxWidth.False">
    <EditForm Model="@StudioDisplayModel" OnValidSubmit="UpdateStudioAsync">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <MudGrid>
            <MudItem xs="12">
                <MudCard>
                    <CreateUpdateStudioComponent StudioDisplayModel="StudioDisplayModel" />
                    <MudCardActions Class="gap-2">
                        <MudButton Variant="Variant.Filled"
                                   Color="Color.Primary"
                                   ButtonType="ButtonType.Submit">
                            Update
                        </MudButton>
                        <MudButton Variant="Variant.Outlined"
                                   Color="Color.Default"
                                   Href="@($"/studio/view/{StudioId}")">
                            Cancel
                        </MudButton>
                    </MudCardActions>
                </MudCard>
            </MudItem>
        </MudGrid>
    </EditForm>
</MudContainer>
